interface:
  display_name: "Stellar Forge"
  short_description: "Repo-specific patterns and workflows for stellar-forge"
  default_prompt: "Use the stellar-forge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true