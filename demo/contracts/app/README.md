# app

Starter Soroban contract scaffold generated for template `basic`.

The generated contract stores an admin and a counter, and exposes `init`, `increment`, `set_count`, `count`, and `admin` so you can evolve it from a compiling baseline.
